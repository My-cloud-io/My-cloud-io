{
  "name": "cloud-zen",
  "version": "3.3.0",
  "private": true,
  "description": "Cloud-Zen Vercel + Telegram private cloud with fast indexed listing, 4MB uploads, file menus, rename/share/delete password and PDF preview.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "telegram:session": "node telegram-session.js",
    "healthcheck": "node --check server.js"
  },
  "engines": {
    "node": "24.x"
  },
  "dependencies": {
    "archiver": "^7.0.1",
    "express": "^5.1.0",
    "teleproto": "^1.229.0"
  }
}
